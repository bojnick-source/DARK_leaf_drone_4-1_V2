# Legacy v1 (Extracted Archive)

Reference-only extracted ZIP snapshots.
NOT wired into builds/tests/CI.

Structure:
- legacy/v1/zips_extracted/<zip_name>/...

Porting policy:
Only selectively port ideas into v2 via small PRs with determinism + schema stability + tests.

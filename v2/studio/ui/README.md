# UI Assets

This folder now contains a static v2 Studio UI preview.

## Files

- `index.html`: UI shell
- `studio.css`: styling
- `studio.js`: sample interactions

## Preview

Serve this directory with a static file server and open `index.html`.
If opened directly from the file system, the UI falls back to embedded sample data.
